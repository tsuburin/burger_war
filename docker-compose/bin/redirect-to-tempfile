#!/bin/bash

if [[ $# -lt 1 ]]; then
  exit 0
fi

stdout=$(mktemp --tmpdir $$.stdout.XXXXXXXXXX)
stderr=$(mktemp --tmpdir $$.stderr.XXXXXXXXXX)

echo "redirect: $* > $stdout 2> $stderr"
exec "$@" > $stdout 2> $stderr
